let newTitle = document.querySelector(".title");
let chosenMember = document.querySelector(".item2");
// newTitle.innerHTML = newTitle.innerHTML + chosenMember.innerHTML;

// Otra opción para escribir menos código +=
newTitle.innerHTML += chosenMember.innerHTML;
